app\: Views layer. Where the pages are 
    - _layout.tsx:
      - RootLayout(): displaying the screen
    - (tabs): we put () so that it doesnt show in the link. So we see \ not \tabs

src\:
  - providers.tsx: where we declare contexts(key, value)
  - domain\:
    - person\:
      - types.tsx: constructor or declaration of the object
      - components\PersonCard.tsx: the display of that object

